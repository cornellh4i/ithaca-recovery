name: Doc Freshness

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check-doc-versions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: .github/scripts/check-doc-versions.sh
